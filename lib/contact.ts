/** Single source of truth for the address shown across the site. */
export const CONTACT_EMAIL = 'simbaconteh1@gmail.com'

export const MAILTO = `mailto:${CONTACT_EMAIL}?subject=${encodeURIComponent(
  'Floranica — access to test the project'
)}`
